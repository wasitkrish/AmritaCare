{
  "version": 2,
  "buildCommand": "npm ci && npm run build",
  "public": true,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
